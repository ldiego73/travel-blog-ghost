{{#foreach navigation}}
<a class="navbar-item navbar-{{slug}}{{#if current}} is-active{{/if}}" href="{{url absolute="true"}}">
  {{label}}
</a>
{{/foreach}}
<a class="navbar-item navbar-more">
  <span class="icon fa-lg">
    <i class="fas fa-ellipsis-h"></i>
  </span>
</a>
